@inject IConfiguration Cfg
@* 不是正式站就掛紅字，免得把測試資料當正式的看。
   看 Database:Target 這個開關，不要去比對連線字串裡的 IP —— 連線字串是秘密，不該讀進 View *@
@if (Cfg["Database:Target"] != "Production")
{
    <div class="h1 text-danger">測試環境</div>
}
